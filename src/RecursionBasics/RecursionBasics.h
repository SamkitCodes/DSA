#pragma once
#include <string>


class RecursionBasics {
public:
    static int calculateFactorial(int n);

    static void reverseArray(int arr[], int index, int length);

    static bool stringPalindromeCheck(std::string& word);
};

